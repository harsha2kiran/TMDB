%h4
  Related images
.span12
  - @images.each (image) ->
    %a.span3{ href: "/#!/images/#{image.get('id')}" }
      .image
        %img{ src: image.get("image_file").small.url, alt: "no image"}
      .title
        = image.get("title")
